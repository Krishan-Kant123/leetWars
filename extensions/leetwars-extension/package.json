{
  "name": "leetwars-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node strip-exports.js && npm run copy-assets",
    "copy-assets": "copyfiles manifest.json popup.html icon.png dist",
    "watch": "tsc -p tsconfig.json -w"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^5.6.3"
  }
}
